import React from "react";

import { Typography } from "@material-ui/core";

import NavDrawer from "./NavDrawer";

const Contacts = () => {
  return (
    <NavDrawer>
      <h1>Contacts</h1>
      <Typography>Just a stub for now</Typography>
    </NavDrawer>
  );
};

export default Contacts;
