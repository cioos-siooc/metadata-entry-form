[project]
name = "cioos-record-schema-tools"
version = "0.1.0"
description = "Documentation and conformance tooling for the CIOOS metadata record JSON Schema"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "json-schema-for-humans>=1.0.0",
    "jsonschema>=4.20.0",
]
