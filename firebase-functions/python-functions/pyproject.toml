[project]
name = "python-functions"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "cioos-metadata-conversion",
    "firebase-functions>=0.4.3",
    "setuptools<81",
]

[tool.uv]
dev-dependencies = [
    "ruff>=0.8.2",
]

[tool.ruff]
target-version = "py311"
line-length = 88

[tool.ruff.lint]
select = ["E", "F", "W", "I", "UP", "B"]

[tool.uv.sources]
# TODO: re-pin to main once feat/load-from-obis is merged. That branch carries the
# load_from/ loaders (datacite, obis, pdc) used by create_record_from_source.
cioos-metadata-conversion = { git = "https://github.com/cioos-siooc/cioos-metadata-conversion.git", branch = "main" }
